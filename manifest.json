{ 
  "manifest_version": 3,
  "name": "HTTPS API Request Tracker",
  "version": "1.0",
  "description": "Tracks HTTPS API requests and logs them to the console.",
  "permissions": [ "webRequest", "webRequestBlocking", "" ],
  "background": { "service_worker": "background.js" }
}
